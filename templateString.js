const nombre = "Jose";

const real = "Sojo";

const salida = nombre + ` ` + real;//Forma de usar antes

const salida2 = `${nombre} ${real}`;//Forma de usar Ahora Template String

console.log(salida);
console.log(salida2);